<UserControl x:Class="Cda.App.UI.DialogListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Surface}">
    <DockPanel>
        <TextBlock x:Name="Header" DockPanel.Dock="Top" Foreground="{StaticResource TextSecondary}"
                   Margin="8,6" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                   TextWrapping="NoWrap"
                   Text="Dialog boxes the target raises will appear here, each with the app function that created it. Start 'Detect dialog caller' or 'Launch &amp; detect dialogs…'." />
        <DataGrid x:Name="Grid" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" GridLinesVisibility="None"
                  Background="{StaticResource Surface}" RowBackground="{StaticResource Surface}" AlternatingRowBackground="{StaticResource SurfaceAlt}"
                  Foreground="{StaticResource TextPrimary}" BorderThickness="0"
                  EnableRowVirtualization="True" EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                  SelectionMode="Single"
                  FontFamily="Cascadia Mono, Consolas" FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="Auto" MinWidth="72" ElementStyle="{StaticResource GridCellText}" />
                <DataGridTextColumn Header="Dialog API" Binding="{Binding Api}" Width="Auto" MinWidth="150" ElementStyle="{StaticResource GridCellText}" />
                <DataGridTextColumn Header="Caption / text" Binding="{Binding Caption}" Width="280" MinWidth="140" ElementStyle="{StaticResource GridCellText}" />
                <DataGridTextColumn Header="Branch" Binding="{Binding Branch}" Width="Auto" MinWidth="120" ElementStyle="{StaticResource GridCellText}" />
                <DataGridTextColumn Header="Created by (caller)" Binding="{Binding Caller}" Width="Auto" MinWidth="180" ElementStyle="{StaticResource GridCellText}" />
                <DataGridTextColumn Header="Module" Binding="{Binding Module}" Width="Auto" ElementStyle="{StaticResource GridCellText}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
