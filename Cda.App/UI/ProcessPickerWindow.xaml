<Window x:Class="Cda.App.UI.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Attach to process"
        Width="560" Height="640"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource Surface}">
    <DockPanel Margin="10">
        <TextBlock DockPanel.Dock="Top" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"
                   TextWrapping="Wrap"
                   Text="Select a process to attach to. Discovery is read-only — it reads the target's modules and code to build the call graph; it does not modify the target." />

        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="Filter" Grid.Column="0" Padding="4"
                     ToolTip="Filter by name or PID" TextChanged="OnFilterChanged" />
            <Button x:Name="Refresh" Grid.Column="1" Content="Refresh" Padding="12,4" Margin="8,0,0,0"
                    Click="OnRefresh" />
        </Grid>

        <Border DockPanel.Dock="Bottom" Margin="0,8,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="AttachBtn" Content="Attach" Padding="18,5" Margin="0,0,8,0" Click="OnAttach" />
                <Button Content="Cancel" Padding="18,5" Click="OnCancel" />
            </StackPanel>
        </Border>

        <ListBox x:Name="List"
                 DisplayMemberPath="Display" FontFamily="Cascadia Mono, Consolas" FontSize="12"
                 MouseDoubleClick="OnListDoubleClick" />
    </DockPanel>
</Window>
